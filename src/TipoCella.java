/**
 * L'<i>enum</i> {@code TipoCella} indica gli stati
 * in cui può trovarsi una {@code Cella} del <i>mondo artificiale</i>.
 * 
 * @author Marco Scanu
 */
public enum TipoCella {
	VISITATA,
	NON_VISITATA,
	BLOCCATA;
}
